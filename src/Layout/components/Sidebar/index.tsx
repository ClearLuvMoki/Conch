import React from "react";
import {Button} from "@nextui-org/react";

const Sidebar = () => {
    return (
        <div className={"w-[64px] h-full bg-PrimaryBackground"}>
            {/*<Button color="primary">1</Button>*/}
        </div>
    );
};

export default Sidebar;
